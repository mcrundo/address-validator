# Google Maps Address Validation API key.
# In local docker-compose, this value is seeded into a fake Secrets Manager
# (moto) on startup. The Lambda fetches it via boto3 at runtime, matching
# the production code path.
GOOGLE_MAPS_API_KEY=your-api-key-here
